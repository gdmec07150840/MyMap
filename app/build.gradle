apply plugin: 'com.android.application'

android {
    signingConfigs {
        MrX {
            keyAlias 'mrx'
            keyPassword 'iambest1996'
            storeFile file('E:/Androidstudio/签名/MrX.jks')
            storePassword 'iambest1996'
        }
    }
    compileSdkVersion 25
    buildToolsVersion "25.0.0"
    defaultConfig {
        applicationId "cn.edu.gdmec.s07150840.mymap"
        minSdkVersion 15
        targetSdkVersion 25
        versionCode 1
        versionName "1.0"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
        debug {
            signingConfig signingConfigs.MrX
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:25.0.0'
    compile files('libs/BaiduLBS_Android.jar')
}
